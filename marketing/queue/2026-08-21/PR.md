One day of marketing drafts for review. Nothing here is published — `index.html`, `assets/` and `main` are untouched.

## Angle

**The perspective flip** — the back of the lid is the one surface *you* never look at, and the one everyone sitting behind you looks at all day.

This is the first entry in `marketing/queue/`, so it sets the rotation baseline rather than differentiating from a previous day. It was chosen partly to avoid doubling up on the coffee-money and ad-space angles, which already have imagery on the live site via `assets/wild-coffee-money.jpg` and `assets/wild-ad-space.jpg`.

## Pin title

> you see one side of your laptop. strangers see the other — and it can pay you

79 chars. Description 482 chars, 7 hashtags — both inside the Pinterest limits.

## Social previews

| Channel | Preview | Length |
|---|---|---|
| X / Threads | *"you stare at one side of your laptop. the café stares at the other."* | 214 / 240 chars |
| Instagram | *"you have never once looked at the back of your own laptop."* | 12 hashtags |
| LinkedIn | *"Every laptop has a side its owner never looks at."* | 5 lines, no hashtags |

Each carries its own `?ref=` tag: `pin-20260821`, `x-20260821`, `ig-20260821`, `li-20260821`.

## Digest suggestions

1. **A "vote with your city" pin next run** — the site's whole CTA is email + city and the FAQ says joining *is* the vote, but no asset makes that mechanic visible. It also gives joiners a reason to recruit, which the referral copy from `acfea02` already rewards.
2. **A "peels off clean" proof asset** — "will the sticker wreck my laptop?" is the biggest objection between interest and signup and it's currently answered in words only. A three-panel peel strip answers it instantly and is reusable on Pinterest, Stories and the FAQ itself.
3. **An angle ledger at `marketing/angles.md`** — each run currently re-reads every queue folder to work out what's been covered. An append-only date/angle/headline file makes rotation a lookup, and becomes the record of which angles actually earned clicks once `?ref=` data lands.

## ⚠️ pin.png is missing

The pin was generated, brand-corrected and exported at 1000 × 1500 in Canva, but the download into the repo failed: this sandbox proxies all outbound HTTPS and denies every Canva host with `403` on CONNECT (`export-download.canva.com`, `design.canva.ai`, `www.canva.com`). The Canva MCP tools worked fine — they call Canva server-side — so this is purely an environment network-policy limit.

**The design is real and ready to download:** `DAHS7IxIjUY` — https://www.canva.com/d/Bu6ZE6XtK_8KB-a

`pin-image-status.md` has the direct export link, three recovery options, and a note that whitelisting `export-download.canva.com` would make this step automatic on all future runs. The `image_prompt` in `pin.md` is a complete no-Canva fallback.

One rejected Canva draft (`DAHS7E646Ck`) was left in the account by the candidate-comparison step and is safe to delete — details in `pin-image-status.md`.

## Compliance

No invented rates, earnings, stats, testimonials, press or partner brands. Earnings are only ever phrased as "$ per verified scan". Currency is `$` throughout. All claims mirror `index.html` and don't exceed it.

---
_Generated by [Claude Code](https://claude.ai/code)_
