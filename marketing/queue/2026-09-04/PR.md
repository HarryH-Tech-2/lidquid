One day of marketing drafts for review. Nothing here has been published — drafts only.

## Angle

**The social / ice-breaker angle** — the lid doesn't just earn, it starts conversations.

This is the first entry in `marketing/queue/`, so it opens the rotation. It deliberately avoids the three themes already represented in `assets/` (coffee money, ad space, the café scan) so those stay available as fresh angles for later days.

## Pin

**Title:** wait, what IS that? — the laptop sticker that pays you per scan

Board suggestion: *Side Hustles for Café Regulars*. Link: `https://lidquid.com/?ref=pin-20260904`

## Social — one-line preview of each

- **X / Threads** — *every café has that moment. someone leans over: "wait, what is that?"*
- **Instagram** — *nobody talks to anyone in a café. everyone's got headphones in and a deadline on — until someone spots the sticker on my lid.*
- **LinkedIn** — *Every laptop lid in a café faces the room. Nobody has ever rented that space.*

Each carries its own ref tag (`x-` / `ig-` / `li-20260904`).

## Digest — the 3 suggestions for what to make next

1. **A "cities open by demand" pin built around the reader's own city** — the only angle where the pin itself is the CTA, since joining the waitlist *is* the vote for your city. Repeatable per city as the pilot expands.
2. **A "peels off clean" objection-killer pin** — answers the biggest silent objection (will this wreck my laptop?) before the reader has to go hunting for the FAQ.
3. **A short vertical video or 3–4 frame carousel of the café moment** — today's angle is a sequence, not a still; a single frame flattens it.

## ⚠️ pin.png is missing

The Canva design was generated, rebuilt in the brand style, committed and exported successfully — but the PNG **could not be downloaded** into the repo. This session's egress proxy returns 403 for both `export-download.canva.com` and `design.canva.ai`, an organization network-policy denial that the environment's own guidance says to report rather than retry or route around.

- Canva design ID: **`DAHUPDjb6i0`** — *"Pinterest Pin - Wait, what IS that?"*, 1000×1500. Open it in Canva, download as PNG, and drop it in as `marketing/queue/2026-09-04/pin.png`.
- The `image_prompt` in `pin.md` is a complete text-to-image prompt and remains the documented fallback.
- Allowlisting `export-download.canva.com` would unblock this end to end for future runs.

Full detail in `pin-image-status.md`.

## Worth a reviewer's eye

The first AI-generated pin was rejected and rebuilt: its embedded illustration rendered garbled, duplicated headline text and an off-voice line ("The perfect café conversation starter for everyone"). The replacement is built from clean vector shapes in the brand palette. Since `design.canva.ai` is blocked, the four AI candidate previews couldn't be compared before picking one.

No earnings figures, rates, stats, testimonials, press mentions or partner brands appear anywhere in these drafts. Currency is `$` throughout. Claims are held to what `index.html` already says.
